{
  "name": "swiss-projection",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
				"make": "browserify lib/for-browser.js -o dist/swiss-proj.js && minify dist/swiss-proj.js -o dist/swiss-proj.min.js"
  },
  "author": "idris-maps",
  "license": "GPLv2",
  "dependencies": {
    "proj4": "^2.3.15"
  }
}
